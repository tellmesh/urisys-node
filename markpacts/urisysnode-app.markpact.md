# UriPack: urisysnode-app

Thin Markpact — capabilities from [`urisys-node/urisysnode/manifest.yaml`](urisys-node/urisysnode/manifest.yaml).
Handlers: existing ``python://`` refs (code in `urisys-node/`, not duplicated here).

Generated by `urisys/scripts/generate_pack_markpacts.py`.

```yaml markpact:pack
apiVersion: urisys.io/v1
kind: UriPack
metadata:
  id: urisysnode-app
  version: 0.1.21
  language: python
description: 'urisys-node slave capability routes (node:// + app://). HTTP transport
  (GET /health, POST /uri/call) is documented separately in markpacts/urisys-node.contract.markpact.md.

  '
schemes:
- app
capabilities:
- id: app-chat-messages
  uri: app://{target}/chat/query/messages
  kind: query
  operation: app.chat.messages
  handler: python://urisysnode.app_handlers:query_chat_messages
  side_effects: false
  approval: not_required
- id: app-chat-channels
  uri: app://{target}/chat/query/channels
  kind: query
  operation: app.chat.channels
  handler: python://urisysnode.app_handlers:query_chat_channels
  side_effects: false
  approval: not_required
- id: app-chat-append
  uri: app://{target}/chat/command/append
  kind: command
  operation: app.chat.append
  handler: python://urisysnode.app_handlers:command_chat_append
  side_effects: true
  approval: required
policy:
  default: deny_mutations_without_approval
runtime:
  default_environment: real
  supports:
  - mock
  - local
  - docker
  expose:
  - pack
  - service
  - flow
  - interface
  - adapter
```

```yaml markpact:run
scheme: app
default: flow
modes:
- pack
- service
- flow
- interface
- adapter
service:
  port_hint: 8789
  path: /uri/call
flow:
  ids:
  - app-smoke
uses: []
adapter:
  call: POST /uri/call
  events: GET /events
```

```yaml markpact:tests
tests:
- id: app.chat.messages_query
  uri: app://local/chat/query/messages
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: app.chat.messages
- id: app.chat.append_command
  uri: app://local/chat/command/append
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: app.chat.append
  payload: {}
```

```yaml markpact:flow id=app-smoke
flow:
  id: app-smoke
  description: Smoke app:// routes (generated from manifest).
defaults:
  approved: true
  dry_run: true
do:
- app://local/chat/query/messages
- app://local/chat/query/channels
- app://local/chat/command/append: {}
```

```markdown markpact:docs
## Unpack → `.markpact/`

```bash
cd urisys-node
export TELLMESH_ROOT=/home/tom/github/tellmesh
PACK=markpacts/urisysnode-app.markpact.md
urisys markpact run "$PACK" --as flow --approve --dry-run
urisys markpact run "$PACK" --as pack
urisys markpact run "$PACK" --as service --port 8789
urisys markpact run "$PACK" --as interface
urisys markpact run "$PACK" --as adapter
```

- Source manifest: `urisys-node/urisysnode/manifest.yaml`
- Requires: `pip install -e urisys-node` or `TELLMESH_ROOT` for python:// handlers
```
